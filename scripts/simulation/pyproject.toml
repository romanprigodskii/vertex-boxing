[project]
name = "vertexboxing-simulation"
version = "0.1.0"
description = "Boxing bout-outcome model (3-way W/D/L) for Vertex Boxing"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]
