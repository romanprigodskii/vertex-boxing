# The versions every file in results/ was computed with (results/environment.json).
# LightGBM's output depends on its version even with `deterministic` set, so pin it.
numpy==2.2.3
pandas==2.2.3
scipy==1.18.1
scikit-learn==1.9.0
lightgbm==4.7.0
pyarrow==24.0.0
# only for building the corpus snapshot from the database (snapshot_corpus.py)
psycopg[binary]>=3.2
python-dotenv>=1.0
